server {
    listen 80;
    root /opt/proactive-monitoring/visualization;
    index dashboard.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8000;
    }
}
